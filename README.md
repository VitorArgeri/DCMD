# DCMD

DCMD is a local Windows launcher with a command-style interface.

Sprint 01 includes:

- initial project structure under `src/dcmd`
- editable JSON configuration in `config/`
- sample authorized scripts in `scripts/`
- command parsing for direct, search, script, and program commands
- automated tests in `tests/`

Planned test command:

```text
pytest
```
